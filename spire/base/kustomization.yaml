apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - zerotrustworkloadidentitymanager.yaml
  - spireserver.yaml
  - spireagent.yaml
  - spiffecsidriver.yaml
  - spireoidcdiscoveryprovider.yaml
  # clusterspiffeid-myclient.yaml removed - using default ClusterSPIFFEID template
  # Default template: spiffe://{{ .TrustDomain }}/ns/{{ .PodMeta.Namespace }}/sa/{{ .PodSpec.ServiceAccountName }}
