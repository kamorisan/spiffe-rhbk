apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount-myclient.yaml
  - jwt-test-client.yaml
  - auth-test-job.yaml
  # jwt-svid-full-auth-test-job.yaml - disabled, initContainer fails (spire-agent binary location)
